{
  "name": "orbita-board",
  "version": "1.0.0",
  "private": true,
  "description": "Painel de projetos de um estúdio de design — dados fictícios, TypeScript sem dependências de runtime.",
  "scripts": {
    "build": "tsc && node build.mjs",
    "watch": "tsc --watch"
  },
  "devDependencies": { "typescript": "^5.6.3" }
}
